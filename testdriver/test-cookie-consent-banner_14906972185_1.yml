version: 4.2.29
session: 681cac9c32fef6c6f161c249
steps:
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Check if the cookie consent banner is displayed.
    commands:
      - command: assert
        expect: the cookie consent banner is displayed
  - prompt: Click the "Got It" button.
    commands:
      - command: hover-text
        text: Got It!
        description: button on the cookie consent banner at the top of the page
        action: click
  - prompt: Assert that the banner disappears after clicking.
    commands:
      - command: assert
        expect: the cookie consent banner is not displayed
